# Direct dependencies only, added task by task as each one is actually needed.
#
#   uv venv
#   uv pip install -r requirements.txt
#   uv pip install -e .          # makes `app` importable from any directory

# T1 - typed configuration loaded from .env
pydantic-settings==2.15.0

# T2 - knowledge-base acquisition (HTML cleanup + markdown conversion).
# The fetcher uses stdlib urllib, not httpx: see app/ingest/fetch_sources.py
# for why. httpx arrives in T6 for the MCP servers.
beautifulsoup4==4.15.0
markdownify==1.2.3

# T3 - heading-aware chunking (pulls langchain-core, which T5 also uses)
langchain-text-splitters==1.1.2

# T4 - embeddings and vector store
fastembed==0.8.0
faiss-cpu==1.15.0
langchain-community==0.4.2

# T5 - retriever + KB tool (langchain-core arrived with T3)

# T6/T7 - MCP servers (weather via Open-Meteo, currency via Frankfurter).
# Pinned to mcp 1.x: langchain-mcp-adapters (T8) requires mcp<2, and the client
# and server sides have to agree on the SDK major version.
mcp[cli]==1.30.0
httpx==0.28.1

# T8 - MCP client, bridging MCP tools into LangChain
langchain-mcp-adapters==0.3.2
